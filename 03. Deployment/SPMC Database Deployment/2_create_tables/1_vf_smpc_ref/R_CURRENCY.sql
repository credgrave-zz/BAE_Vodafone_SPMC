--------------------------------------------------------
--  DDL for Table R_CURRENCY
--------------------------------------------------------

  CREATE TABLE "VF_SPMC_REF"."R_CURRENCY" 
   (	"CURRENCY_CD" VARCHAR2(50), 
	"CURRENCY_DESC" VARCHAR2(256), 
	"CREATION_DT" DATE, 
	"CURR_FLAG_YN" CHAR(1), 
	"VALID_FROM_DT" DATE, 
	"VALID_UNTIL_DT" DATE
   ) ;
