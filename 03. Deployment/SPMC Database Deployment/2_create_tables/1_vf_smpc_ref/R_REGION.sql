--------------------------------------------------------
--  DDL for Table R_REGION
--------------------------------------------------------

  CREATE TABLE "VF_SPMC_REF"."R_REGION" 
   (	"REGION_CD" VARCHAR2(50), 
	"REGION_DESC" VARCHAR2(256), 
	"CREATION_DT" DATE, 
	"CURR_FLAG_YN" CHAR(1), 
	"VALID_FROM_DT" DATE, 
	"VALID_UNTIL_DT" DATE
   ) ;
